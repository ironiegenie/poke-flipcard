import { KartenDatenBuilder } from './karten-daten-builder';

describe('KartenDatenBuilder', () => {
  it('should create an instance', () => {
    expect(new KartenDatenBuilder()).toBeTruthy();
  });
});
